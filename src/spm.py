# this code is NOT COMPILED. I solely added this so syntax highlighting would work in your IDE and so you aren't coding python inside of a c++ string. Copy+paste the code into the string in py::exec in main.

import aria

def f(argc, argv)
    pass
    # aria.call(uris)
